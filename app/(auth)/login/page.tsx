import { LoginForm } from '@/components/auth/LoginForm';

export const metadata = {
  title: 'Login - Smart LMS',
  description: 'Sign in to your Smart LMS account',
};

export default function LoginPage() {
  return <LoginForm />;
}

